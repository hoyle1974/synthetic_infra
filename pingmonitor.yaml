apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: ping-v1
  labels:
    name: ping-v1
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      name: ping-v1
  podMetricsEndpoints:
  - targetPort: 8080
