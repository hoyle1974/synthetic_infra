apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: gitea-sm
  labels:
    env: production
spec:
  selector:
    matchLabels:
      app: gitea
        #namespaceSelector:
        #matchNames:
        #- gitea
  endpoints:
    - port: http
      path: /metrics
